let base = prompt('Hola ingresa la base del cuadrado');
let altura = prompt('Hola ingresa la altura del cuadrado');

const area = (base, altura) => {
    let resul = base * altura;
    alert(resul);
}

area (base, altura);